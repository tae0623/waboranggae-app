# ver5 배포 기록

2026-09-16 KST. 기존 ver4의 `waboranggae/` 폴더 구조를 유지한다.

- Supabase 프로젝트 `drtxexwznmpmiclvrjji`의 `waboranggae-api`에 코드만 재배포. 비밀값 재전송, DB 마이그레이션, 공개 전환 없음.
- 서버 번들: `315a8badb21a35a7`. 비공개 검증 모드 유지.
- 배포 후 과대 요청 응답 차이를 확인해 Content-Length 사전 제한을 추가했다. 길이가 없는 요청은 기존 파서의 128KB 제한으로 검사한다. 배포 직후 일부 500 응답이 있어 재확인했다. 완료 판정은 아래 smoke 검사 결과 기준이다.
- 로컬 테스트: 49 파일 / 406개 통과, TypeScript 통과. 직전 Android 검사 79개 통과, lint 오류 0, debug APK 빌드 성공.
- 최종 배포 후 smoke 검사: 2026-09-16 17:47 KST, 12개 검사 전체 통과. 잘못된 JSON 400·과대 요청 413은 각각 3회 연속 확인했다.
- S20+에 `adb install -r`로 재설치하고 앱 실행·로그인 화면 표시 확인. 기존 앱 데이터/계정/권한을 지우지 않았다. 실계정 로그인은 자동으로 수행하지 않았다.
- 설치 APK SHA-256: `A601604F0C1F0B15D5A7BED39CAED6FD99528892A02D308ABB4C8CD75EF0DD77`.
- GitHub에는 소스·예제 설정·DB 마이그레이션 파일·테스트·문서만 포함. `.env`, 개인 local.properties, 서명 키, APK, node_modules, 런타임/생성 번들은 제외.

검증 명령: 프로젝트 폴더에서 `node scripts/verify-deploy-smoke.mjs`.
게이트/계정 인증, 잘못된 JSON 400, 과대 요청 413, 하단 출처 안내를 확인한다. 계정 생성/삭제와 관광·길찾기 API 호출은 하지 않는다.

**제한:** 카카오 일일 보호 한도는 변경하지 않았다. 휴대폰용 임시 검증 접속은 **2026-09-16 23:32:11 KST**에 만료된다. 원스토어 공개 출시본이 아니며 공개 배포 판단은 [출시 전 검토](PRE_RELEASE_AUDIT_2026-09-16.md)의 잔여 항목을 해결한 뒤 진행한다.
